# Plan: {{change-name}}

> Generated by sdd-plan | {{date}}

## 批次划分

<!-- 按独立性拆分批次，批次间无文件交叉修改 -->

| 批次 | 任务 | 变更文件 | 依赖批次 |
|------|------|----------|----------|
| batch-1 | | | - |

## Batch 1: {{batch-name}}

### 任务

<!-- 每个 task 精确到 2-5 分钟工程师操作级 -->

1. **{{task}}**
   - 文件：`{{path}}`
   - 测试：`{{test-command}}`
   - 验证：`{{verify-command}}`

2. **{{task}}**
   - 文件：`{{path}}`
   - 测试：`{{test-command}}`
   - 验证：`{{verify-command}}`

### 验证检查点

```bash
{{verification-commands}}
```

## Batch 2: {{batch-name}}

<!-- 同上结构 -->

### 任务

1. **{{task}}**
   - 文件：`{{path}}`
   - 测试：`{{test-command}}`
   - 验证：`{{verify-command}}`

### 验证检查点

```bash
{{verification-commands}}
```

## 回滚策略

| 批次 | 回滚方式 |
|------|----------|
| batch-1 | `git revert` |
